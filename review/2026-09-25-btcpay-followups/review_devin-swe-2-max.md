Now I need to survey the full source for remaining `ss.getItem('wallet')` / `getWallet()` readers, the `btc-wallet-password` class usage, and `checkBtcpayTransactions`.Let me see the rest of the wiring test file and the URI-handler unlock path.A few more reads: `dialogLogout` (does it take a password?), where `dialogPassword(true)` and `dialogPassphrase` are invoked, `skipWalletAuth` readers, and the `FW`/queue init and `updateNetworkInfo`.One more look at `resetWallet` and `dialogLock` to close out the remaining-reader sweep.## Findings

**1. `checkBtcpayAuth` still decides on the swapped seed (line 1135).** `b = ss.getItem('wallet')` is the remaining reader that treats a hot-swapped stash as "wallet open." If `enabled && a==null && b`, it re-creates `btcpayWallet`/`btcpayKeys` from whatever sits in `ss.wallet` (1136–1140). Reachable residual: user hits Disable in `dialogEnableBtcpay` → `disableBtcpayAutopay()` clears the stash (1158–1159) while a `cpBtcpay` call is in flight with the swap live → a subsequent `checkBtcpayAuth` re-stashes the very seed copy the user just removed. `checkBtcpayAuth` currently runs only from launch-dialog callbacks (329, 332), so the window is narrow, but the function's contract is wrong on exactly the state this patch isolates. The `a`/`b` reads in `processBtcpayQueue`/`autoBtcpay` (1312, 1345) are intentional — a swapped seed legitimately satisfies `o.autopay && (a||b)`.

**2. `isPasswordDialogOpen` is a naming contract, fail-open.** All four password dialogs carry `btc-wallet-password` (4220, 4272, 4347, 4997) — verified — but nothing enforces it; any future password prompt without the class silently unholds the queue. `dialogWelcome`, `dialogLogout` (checkbox, no password), license/donate dialogs take no password, so no current bypass. Stuck-holding: `dialogMigrate` has no Cancel and `closable:false`; an unrecoverable vault ("could not be decrypted" retry loop, 4423) holds the manual prompt forever — defensible, since payment needs unlock anyway. False-while-up: not reachable — the launch `dialogPassword` is realized synchronously before `checkUpdateWallet()` runs (329→338), and the URI-path `dialogPassword` (5450) carries the class. One race: between the URI unlock dialog's `close()` and its callback reopening the send/sign dialog, a 60 s tick can interleave `dialogBTCpay` — cosmetic ordering, `#btcpay-form` dedupe still applies.

**3. `cleanupBtcpay` throws before network info loads (1242).** `FW.NETWORK_INFO` inits to `{}` when `ls.networkInfo` is absent (25); `FW.NETWORK_INFO.network_info[network].block_height` then throws TypeError if a persisted non-empty `btcpayQueue` (101) exists on a launch before `updateNetworkInfo` returns (1866–1870). Via the synchronous `else` branch (1224) the exception propagates through `checkUpdateWallet` into `initWallet`, killing `setInterval`/`updateWalletOptions`. Pre-existing, fails closed, but now on the exercised path.

**4. Auto-lock: no residual hole.** `FW.WALLET_ENCKEY` is only set by verified-password paths that also set `ss.wallet` (563–565, 659–661), so an unlocked session always satisfies `isWalletUnlocked()`; unlock-during-swap yields both and normal autolock. The old behavior — `ss.getItem('wallet')` true on the stash, autolock firing mid-`cpBtcpay` on a stale `WALLET_LAST_UNLOCKED`, yanking `ss.wallet` under a signing call — was the bug being removed, not relied-on behavior.

**5. Test gate is real but shallow where it matters.** `execFileSync` throws on non-zero exit, so a failing script fails `npm test`; 14-min kill inside 15-min test timeout handles hangs. Gaps: `isPasswordDialogOpen` is stubbed (`__passwordDialogOpen`), so the class contract in finding 2 is unverified; the `$` stub lacks `.length`, making the `$('#btcpay-form')` bail (1329) silently unreachable; `autoBtcpay` is a spy, so the hot-swap/`c && !FW.WALLET_ENCKEY` cleanup (1358–1384) — the state `isWalletUnlocked` exists to distinguish — is never executed; `dialogPassphrase`'s new gate (4458) isn't extracted or tested.

**Minor:** `changeWalletPassword`'s `!getWallet()` check (591) passes on a swapped seed, but the verified current password is the real auth and it sets `ENCKEY` anyway — cosmetic. `index.html:232` reads `ls`, not session — correct.
