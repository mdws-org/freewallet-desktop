Reads of `/Users/bemeadows/Projects/freewallet-desktop/` were refused by the session's allowed-directory scope, so this is from `diff.patch` + `context-functions.txt` only. The two items marked **[unverified]** name the single check that settles each. Nothing was run.

**1. `isWalletUnlocked()` (688) is false for a legitimately open, unencrypted wallet.** It requires `FW.WALLET_ENCKEY`. It now gates send/privkey/passphrase/auto-lock. If a session with `FW.WALLET_ENCRYPTED` 0, or any unlock path that populates the seed without a derived key, leaves `WALLET_ENCKEY` null, every gate refuses a wallet the old code opened — a lockout regression on the ordinary path, not the swap window. **[unverified]** Check that *every* successful unlock (`isValidWalletPassword`→`decryptWallet`, plus the unencrypted load branch) assigns `WALLET_ENCKEY`. One direction only (lockout, not disclosure), but it is the widest-blast-radius item here.

**2. `isPasswordDialogOpen()` (4614) trusts a class I cannot see on two of the five dialogs it names.** Only `dialogPassword` carries `cssClass: 'btc-wallet-password'` (4347) in the excerpt. `dialogMigrate` and `dialogEnableBtcpay` are asserted only in the comment (4610–4612). If either lacks it, #11 survives verbatim: launch on a legacy wallet → `dialogMigrate(checkBtcpayAuth)` (332) → 60 s tick → `processBtcpayQueue` → guard false → `dialogBTCpay(false)` → `dialogCheckLocked` → "Wallet Locked!" stacked on the migrate dialog, every tick. **[unverified]** `grep -c btc-wallet-password` must be ≥5.

**3. The guard's failure direction is a silently unpaid matched order.** `processBtcpayQueue` returns at 1335 while a password dialog is visible, and nothing re-runs the queue when a dialog closes — the only retry is `checkUpdateWallet`'s 60 s interval (340). `dialogPassword` is `closable:false, closeByBackdrop:false` (4348–4349), i.e. no affordance but its own buttons. If any path opens an unlock/enable dialog whose buttons are unreachable (stacked behind another modal), the query stays true indefinitely and the queued match is never offered. Old symptom: a noise dialog. New symptom: an expired DEX match (`expire_index` in the harness fixture) with no error shown. A re-check on dialog close is worth more than the next tick.

**4. Ordering (Q4).** `checkBtcpayAuth` can itself open `dialogEnableBtcpay()` (1142), so it can return with a `btc-wallet-password` dialog on screen. If `checkBtcpayTransactions()` then reaches `processBtcpayQueue`, 1335 suppresses the manual prompt and finding 3 applies — auth-first is the right order, but only a post-close re-run makes it safe. Separately, the hook runs immediately after unlock, before any network response; check whether the queue path dereferences state a later poll fills. `autoBtcpay` already reads `FW.NETWORK_INFO.fee_info.optimal` unguarded (1349), so an early call with `fee_info` absent yields `NaN` fees rather than an exception.

**5. Residual raw readers (Q1).** `checkBtcpayAuth` still branches on `ss.getItem('wallet')` (1135) — and writes: it copies `ss.wallet`→`btcpayWallet` and `FW.WALLET_KEYS`→`btcpayKeys` (1138–1140). Reached mid-swap, it would persist `FW.WALLET_KEYS` as whatever a concurrent `autoBtcpay` restored, or the `{}` its cleanup assigns at 1384 — losing imported keys and breaking later autopay for imported-address orders. In the seed direction it is benign (the stash is the user's own already-accepted seed), so this is a correctness loss, not disclosure. The raw reads in `autoBtcpay` (1312, 1345) and `processBtcpayQueue` (1312) are the swap mechanism itself and should stay.

Lock-icon handler and `updateWalletOptions` (966) now agree; `checkAutoLock` (1094) skipping during a swap delays the lock by at most one interval while removing the real hazard — `ss.removeItem('wallet')` landing under an in-flight `cpBtcpay`. Correct trade.

**6. Tests.** The gate is real: `execFileSync` throws on non-zero exit, so a failing tools script fails `npm test`. Gaps: success is asserted only as `>0` (a script that skips every check and prints "1 checks passed" passes); on failure the thrown error's message omits the script's stdout/stderr, which is where its diagnostics live; and `isPasswordDialogOpen` is stubbed by a switch, so the DOM semantics deciding finding 2 — and the `WALLET_ENCKEY` question in finding 1 — are outside the harness entirely.
